{
  "name": "action-app-installation-pat",
  "version": "1.0.0",
  "description": "GitHub action to generate a GitHub Personal access token for an App installation",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc && ncc build"
  },
  "devDependencies": {
    "@types/node": "^18.11.3",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "glob": "^8.0.3"
  },
  "engines": {
    "node": ">= 14"
  }
}
